<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="container">
        <h3 class="text-center my-4"><%= title %></h3>
        <div class="row d-flex justify-content-center">
            <div class="col-md-9">
                <div class="card my-3 movie-card">
                    <div class="row g-0">
                      <div class="col-md-2">
                        <img src="/static/image/<%= movie.movieimg %>" class="img-fluid rounded-start h-100 w-100" alt="/static/image/<%= movie.movieimg %>" >
                      </div>
                      <div class="col-md-10">
                        <div class="card-body">
                          <h5 class="card-title moviename"><%= movie.moviename %></h5>
                          <p class="my-3">adlı, <b><%= movie.relasedate %></b> vizyon tarihli, 
                            <b><% movie.categories.forEach(category => { %>
                                <%= category.categoryname %>,
                            <% }) %></b> kategorisindeki filmi silmek istediğinize emin misiniz?</p>
                          <div class="buttons mt-4 d-flex float-end">
                            <form method="post">
                              <input type="hidden" name="movieId" value="<%= movie.id %>">
                              <input type="hidden" name="moviename" value="<%= movie.moviename %>">
                              <button type="submit" class="btn btn-danger px-4">Sil</button>
                            </form>
                            <a href="/admin/movies" class="btn btn-primary ms-2">Vazgeç</a>
                          </div>
                        </div>
                      </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/script') %>
</body>
</html>