<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>


    <div class="container mt-5">
        <div class="row">
            <h3 class="text-center mb-4"><%= title %></h3>
            <div class="col-md-12">
                <form method="post">
                    <div class="card">
                        <div class="row g-0">
                          <div class="col-md-12">
                            <div class="card-body">
                              <input type="hidden" name="categoryid" value="<%= category.id %>">
                              <input type="hidden" name="categoryname" value="<%= category.categoryname %>">
                              <h4 class="card-title">"<%= category.categoryname %>"</h4>
                              <p class="card-text">adlı kategoriye ait <b><%= count %> adet film</b> var yine de bu kategoriyi silmek istiyor musunuz?</p>
                              <div class="button float-end my-2">
                                <button type="submit" class="btn btn-danger px-4 me-2">Sil</button>
                                <a href="/admin/categories" class="btn btn-primary px-4">Vazgeç</a>
                              </div>
                            </div>
                          </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('../partials/script') %>
</body>
</html>