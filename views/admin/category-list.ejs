<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>


    <div class="container mt-5">
        <div class="row">
            <% if (typeof message !="undefined") { %>
                <div class="alert alert-<%= message.class %>">
                    <%= message.text %>
                </div>
            <% } %>
            <%- include('../partials/search-bar') %>
            <table class="table table-striped table-hover table-bordered">
                <thead>
                    <th>#id</th>
                    <th>Kategori Adı</th>
                    <th style="width: 170px; text-align: center;"><a href="/admin/category/create" class="btn btn-sm btn-success">+ yeni kategori ekle</a></th>
                </thead>
                <% categories.forEach(category => { %>
                 
                    <tbody class="tbody">
                            <th>#<%= category.id %></th>
                            <td><%= category.categoryname %></td>
                            <td>
                                <div class="buttons text-center">
                                    <a href="/admin/category/edit/<%= category.id %>" class="btn btn-primary">Düzenle</a>
                                    <a href="/admin/category/delete/<%= category.id %>" class="btn btn-danger">Sil</a>
                                </div>
                            </td>
                    </tbody>
                <% }) %>
            </table>
            <div class="row add-new-item">
                <a href="/admin/category/create" class="text-muted text-center">+ yeni kategori oluştur</a>
            </div>
        </div>
    </div>

    <%- include('../partials/script') %>
</body>
</html>