<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>


    <div class="container mt-5">
        <div class="row">
            <h3 class="text-center mb-4"><%= title %></h3>
            <div class="col-md-12">
                <form method="post">
                    <div class="mb-3 row">
                        <label for="saloonname" class="col-sm-1 col-form-label">Salon Adı:</label>
                        <div class="col-sm-11">
                          <input type="text" class="form-control" id="saloonname" name="saloonname">
                        </div>
                    </div>
                    <div class="mb-3 row">
                        <label for="capacity" class="col-sm-1 col-form-label">Kapasite:</label>
                        <div class="col-sm-11">
                          <input type="number" class="form-control" id="capacity" name="capacity">
                        </div>
                    </div>
                    <div class="mb-3 row">
                        <label for="capacity" class="col-sm-1 col-form-label">Salon tipi:</label>
                        <div class="col-sm-11">
                            <select class="form-select" name="saloontypeid">
                                <% saloontypes.forEach(saloontype => { %>
                                    <option value="<%= saloontype.id %>"><%= saloontype.typename %> (<%= saloontype.info %>)</option> 
                                <% }) %>
                              </select>
                        </select>
                        </div>
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary px-4">Ekle</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include('../partials/script') %>
</body>
</html>